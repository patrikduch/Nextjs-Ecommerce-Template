import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#F8FBFB",
    },
    secondary: {
      main: "#0982E3",
    },
    background: {
      default: "#F3F3F3",
    },
  },
});

export default theme;
