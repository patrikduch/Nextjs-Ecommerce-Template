/* eslint-disable no-unused-vars */

/**
 * @enum All action types that can be used for projectdetail Redux store updates.
 */
enum ActionTypes {
    fetchProjectDetailSuccess = 'fetchProjectDetailSuccess',
}

export default ActionTypes;